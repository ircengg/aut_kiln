[project]
name = "gmr-project-host"
version = "0.1.0"
description = "Lightweight FastAPI host for static inspection viewer projects."
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.116.0",
  "python-multipart>=0.0.20",
  "uvicorn>=0.35.0",
]

[tool.uv]
package = false
